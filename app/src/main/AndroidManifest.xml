<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.priya.shapes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Ex1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Start_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Two_d" />
        <activity android:name=".Three_d" />
        <activity android:name=".Shapes" />
        <activity android:name=".Conversion" />
        <activity android:name=".Calculator" android:exported="true"/>
    </application>

</manifest>